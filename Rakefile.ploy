# Build operations environment

DESTDIR = ENV['DESTDIR'] || '/opt/operations'

base_directories = [
  "#{DESTDIR}/etc/puppet",
  "#{DESTDIR}/var/lib/puppet",
]

base_directories.each do |dir|
  directory dir
end

task :install => base_directories do
  sh "#{File.dirname(__FILE__)}/install-puppet.rb --server ningyo.dc1.puppetlabs.net --installdir #{DESTDIR}"
end
